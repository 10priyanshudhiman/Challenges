#include<stdio.h>
int main()
/*{
    int x,y;
    x = y >7?25 : 50;
    printf("%d %d",x,y);

}
{
    enum Weekday{sunday,monday,tuesday,wednesday,thusday,friday,saturady};
    enum Weekday myDay = monday;

    switch (myDay)
    {
    case sunday:
        printf("myDay is sunday");
        break;
    case monday:
        printf("myDay is monday");
        break;
    default:
        printf("No ONe is my day");

    }
  return 0;
}
{
    unsigned long long sum = 0LL;//Stores the Sum of the Integers.
    unsigned int count = 0;//The number of Integer to be summed.

    //Read the number of integers to be summed.
    printf("\n The number of integer you want to sum: ");
    scanf(" %u", &count);

    //Sum integers from 1 to count.
    for(unsigned int i =1; i <= count; ++i)
        sum += i;
    printf("\nTotal of the first %u numbers is %llu",count , sum);

    return 0;
}
{
    for(;;)// for infinte loop.
    {
        printf("iii");// block of statements;
    }
}*/
{

}
